module.exports = {
    name: 'randomtext',
    description: "hmm",
    execute(message, args){
        
        if (cmd === "david"){
            message.content.send(`Too busy working in Dah Sing`)
        }
        if (cmd === "frank"){
            message.content.send(`go away! *Tears Eden's spam emails. Autocognita is worth more than you!!!!!`)
        }
        if (cmd === "carrie lam"){
            message.content.send(`下台`)
        }
        if (cmd === "carrie lam"){
            message.content.send(`Joe Mama`)
        }
        if (cmd === "mark"){
            message.content.send(`Convoy is reaching bankruptcy...`)
        }
        if (cmd === "ming'"){
            message.content.send(`Ming Is The Richest None The Less!`)
        }
        if (cmd === "susan"){
            message.content.send(`how did you know my name?`)
        }
        if (cmd === "steven"){
            message.content.send(`When i was your age, all i cared about was tennis and girls`)
        }
        if (cmd === "clown"){
            message.content.send(`munching popcorn`)
        }
        if (cmd === "stephen"){
            message.content.send(`Am i relevant?`)
        }
        if (cmd === "uwu"){
            message.content.send(`CRINGE`)
        }
        if (cmd === "ernest"){
            message.content.send(`abusing every power i have`)
        }
        if (cmd === "joe biden"){
            message.content.send(`2020`)
        }
        if (cmd === "donald trump"){
            message.content.send(`2020`)
        }
        if (cmd === "haha"){
            message.content.send(`:)`)
        }
        if (cmd === "h"){
            message.content.send(`ligonese!`)
        }
        if (cmd === "pls hunt"){
            message.content.send(`get your rifle, shoot down the Nazis, and eat them alive! HAHAHA!`)
        }
    }
    
}